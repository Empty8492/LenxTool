# 开发约定

本仓库按“窄范围、测试先行、文档同步、可回滚提交”增量交付。修改前先核对当前路线图、现有调用链和邻近测试；修改后先跑聚焦验证，再跑与影响范围相匹配的完整门禁。

## 中文注释规范

新增或修改的代码必须包含中文注释。注释应说明代码无法直接表达的意图、业务边界和设计原因，至少覆盖：

- 公共类型、关键方法和新增模块的职责；
- 安全、权限、隐私、并发、取消、重试和兼容性边界；
- 非显而易见的算法、状态转换、回退路径及测试夹具目的；
- XAML 接线、配置或迁移中容易被误删、误改的约束。

中文注释不是逐行复述代码。自解释的赋值、简单条件和样板接线不要求机械注释；禁止用无信息量的注释掩盖命名不清或结构过度复杂。行为变化时必须同步更新相关注释，过期注释按缺陷处理。

## 测试与交付

1. 行为变化先写失败测试，确认 RED 后再以最小实现转为 GREEN。
2. 先运行聚焦测试，再运行 Release 全量测试和构建；WPF 运行时测试必须串行。
3. 只提交当前任务文件，不混入用户的相邻改动、凭据、本地配置或构建产物。
4. 功能、验收标准或当前里程碑变化时，同步更新路线图、项目指南和测试报告。
5. 推送后核对远端 SHA、分支 ahead/behind 和工作树状态，不以单次 `git push` 输出代替发布证明。
