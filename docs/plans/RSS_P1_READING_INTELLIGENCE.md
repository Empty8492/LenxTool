# P1 详细计划：阅读增强、AI 与自动化

状态：P1-01～P1-05 基础代码切片已完成；P1-A 仍待真实 WPF 手测与重启验收，P1-06 图片下载接入待实现
最后核对：2026-07-24
开始条件：P0 最终检查点通过，`FeedEntry`、目录版本和角色契约已稳定。
参考项目：RSSNext/Folo（摘要、翻译、规则、全文和媒体转写行为参考），LenxTool 当前 DeepSeek、原生阅读器、FTS5、媒体工作台（实现基础）

## P1 完成定义

普通用户可高质量阅读管理员发布的 Feed：拥有本地私人已读/收藏/标签/备注/进度、离线图片、受控全文、摘要/翻译和统一搜索；管理员可以发布自动化规则和 AI 策略；音视频附件可进入现有媒体工作台。所有内容处理默认发生在本机或现有受控 AI 请求中，云端目录不保存新闻正文和 AI 结果。

## 阶段 P1-A：私人阅读状态

### P1-01：私人阅读状态 schema 与仓储

**目标：** 为 Feed 条目建立本地 `user_entry_states`，统一已读、收藏、进度和私人备注。

**依赖：** P0 `feed_entries`。
**预计范围：** M。
**主要文件：** `SqliteDatabase.cs`、新 `IEntryStateRepository.cs`、实现、SQLite 测试。

**验收：**

- [x] `(entry_id, local_profile)` 唯一；字段包含 read/starred、progress、note、updated_at。
- [x] 更新采用局部 patch，不因标记已读覆盖备注或收藏。
- [x] Feed 软删除/清理不删除仍有私人状态的条目。

**验证：** 并发局部更新、重启往返、profile 隔离、外键/清理和迁移测试。

**参考：** Folo 条目交互状态；LenxTool 现有 `favorites` 表和 180 天保留规格。现有空表需要兼容迁移，不直接丢弃。

**完成记录（2026-07-24）：** P0-20 的 `user_entry_states` schema v6 与仓储基础已补齐完整验收：状态按 `(entry_id, local_profile)` 隔离，局部 patch 保留独立字段，进度/备注边界受约束；并发局部 patch、数据库重开后的状态恢复、Feed 清理保护和 v2/v4 迁移均有集成测试。该仓储不建立 Feed 外键，也不改变共享目录版本。P1-02～P1-04 的代码切片已在后续提交完成。

### P1-02：收藏、标签和备注仓储闭环

**目标：** 补齐项目已建表但缺少的通用仓储能力，并与 Feed 条目状态对齐。

**依赖：** P1-01。
**预计范围：** M。
**主要文件：** 新 `IFavoriteRepository.cs`、`FavoriteRepository.cs`、现有 `INewsRepository` 调整、SQLite 测试。

**验收：**

- [x] Feed、旧早报、热点和 AI 报告都可收藏/取消收藏和编辑备注。
- [x] 标签名称规范化、大小写唯一，实体关联事务化。
- [x] 删除标签不删除实体或私人备注。

**验证：** 多实体往返、重复标签、事务回滚和清理保护测试。

**参考：** Folo 收藏/保存交互；LenxTool 当前 `favorites`、`tags`、`entity_tags` schema。

**完成记录（2026-07-24）：** 扩展 `IFavoriteRepository`/`FavoriteRepository`，以参数化 SQL 和事务支持通用实体收藏、备注更新、批量收藏读取、标签创建/颜色更新、实体标签原子替换和标签删除。收藏实体类型覆盖 Feed、旧早报、热点与 AI 报告的稳定字符串 ID；标签名称先做 NFKC+Trim 规范化并受长度/控制字符限制，数据库已有 NOCASE 唯一约束继续防止重复。删除标签只级联 `entity_tags` 关联，不删除实体或 favorites 私人备注。新增跨实体批量读取、备注更新、标签规范化、原子关联、删除保护和边界测试；P1-03 UI 已在后续代码切片中完成。

### P1-03：阅读状态、收藏和标签 UI

**目标：** 在时间线、阅读器和历史页提供一致的私人状态操作。

**依赖：** P1-01、P1-02。
**预计范围：** M。
**主要文件：** `NewsCenterViewModel.cs`、`HistoryViewModel.cs`、`MainWindow.xaml`、App 测试。

**验收：**

- [x] 打开条目可标记已读，手动可切换未读；列表支持未读/收藏/标签筛选。
- [x] 备注编辑有长度上限、保存状态和取消；不写入共享目录。
- [x] 普通用户可使用私人状态，但无法借此修改 Feed/分类。

**验证：** ViewModel 命令、失败回滚、键盘/焦点、离线和真实 WPF 手测。

**参考：** Folo 阅读状态体验；LenxTool 现有资讯/历史页面风格。

**阶段记录（2026-07-24）：** 已完成 P1-03 的 Feed 时间线交互、私人筛选与历史页一致入口代码切片：每页批量水合通用 favorites，阅读器可收藏/取消收藏、编辑最长 4000 字（支持换行）的私人备注、创建/关联/移除最长 80 字的标签，并显示明确的本机保存、取消或失败状态。首次选择未读条目会持久化已读，仍可手动恢复未读，动作按钮随状态变化。备注不会隐式改变收藏，未保存编辑不会被其他状态操作覆盖；跨 favorites 与 `user_entry_states` 的写入失败会恢复原值，切换条目期间的异步备注/标签写入仍绑定原条目。时间线可组合筛选未读/已读、收藏和标签，SQLite 在分页前按显式 local profile 应用条件，收藏兼容 favorites 与旧 is_starred。统一搜索选中 Feed 结果后，历史右栏提供相同的已读、收藏、备注和标签操作，非 Feed 结果不开放该编辑器。选中条目切换使用代次保护，旧标签异步结果不会覆盖新选择；异步命令取消继续传播。新增成功往返、自动已读/恢复未读、备注取消、独立备注、失败补偿、切换竞态、筛选组合、历史入口、边界和 XAML 键盘/自动化名称测试。真实 WPF 手测仍待完成。

### P1-04：阅读进度与恢复

**目标：** 保存长文阅读位置，并在同一设备恢复。

**依赖：** P1-01。
**预计范围：** S。
**主要文件：** `FeedTimelineBrowserView.xaml(.cs)`、`NewsCenterViewModel.Timeline.cs`、阅读状态测试。

**验收：**

- [x] 进度节流写入，0～100% 合法；短文不产生频繁写入。
- [x] 重开条目恢复位置，并提供“从头阅读”。
- [x] 减少动画模式下恢复不使用滚动动画（原生 `ScrollViewer` 使用直接偏移恢复）。

**验证：** 节流/边界测试、真实长文 WPF 手测。

**参考：** 通用阅读器行为；此项主要源于 LenxTool 本地优先目标。

### 检查点 P1-A

- [ ] 已读、收藏、标签、备注和进度均在重启后保留（代码与自动化测试已覆盖，仍需真实 WPF 重启验收）。
- [ ] 180 天清理不会删除有私人状态的内容。
- [ ] 普通用户的私人状态无法改变共享目录版本。

## 阶段 P1-B：全文与离线资源

### P1-05：离线资源索引与缓存预算

**目标：** 建立 `entry_assets` 和文件缓存服务，补齐当前封面图离线缺口。

**依赖：** P0 条目 schema。
**预计范围：** M。
**主要文件：** `SqliteDatabase.cs`、新 `IEntryAssetStore.cs`、实现、文件/SQLite 测试。

**验收：**

- [x] 资源以内容哈希命名，临时文件原子转正，数据库记录 MIME/大小/来源/访问时间。
- [x] 全局预算、单资源上限和 LRU 清理可配置；被当前文章引用的内容哈希不会在清理中删除。
- [x] 缓存损坏可检测并从索引移除，正文文本读取不依赖缓存文件。

**验证：** 重复资源、磁盘满、取消、损坏、LRU 和中文路径测试。

**参考：** Folo 本地缓存思路；LenxTool `ArticleImageDownloader` 的协议/MIME/12 MiB 基线。

**阶段记录（2026-07-24）：** schema v7 新增 `entry_assets`，`IEntryAssetStore` 以 SHA-256 内容哈希作为物理文件名，写入先落临时文件并在完整读取后转正；数据库记录条目、来源 URL、MIME、大小、创建时间和最近访问时间。缓存选项支持全局预算与单资源上限，按内容哈希去重并按最近访问时间清理，调用方可保护当前阅读内容；读取时重新校验大小和哈希，损坏文件会从索引移除并等待后续重新下载。新增中文内容、超限无残留、LRU 和损坏检测测试；图片下载器接入留在 P1-06。

### P1-06：安全图片下载与本地回退

**目标：** 将封面和正文图片下载到本地缓存，阅读器优先本地、失败回退远端占位。

**依赖：** P1-05。
**预计范围：** M。
**主要文件：** `ArticleImageDownloader.cs`、`ArticleImageBlockFactory.cs`、资源缓存适配、App 测试。

**验收：**

- [ ] 沿用并加强 P0 SSRF/DNS/重定向策略，拒绝 SVG 脚本风险和伪装 MIME。
- [ ] 下载并发、总带宽和每篇资源数受限；取消传播。
- [ ] 完全离线时已缓存图片可显示，未缓存图片给出稳定占位而不反复请求。

**验证：** 私网重定向、错误 MIME、超限、缓存命中和离线 WPF 测试。

**参考：** LenxTool 当前图片下载器/富文本阅读器；Folo 只作为离线体验参考。

### P1-07：全文提取契约与实现评估

**目标：** 以独立 `IArticleContentExtractor` 隔离网页下载、编码识别、正文提取和净化。

**依赖：** P0 URL 安全策略。
**预计范围：** M。
**主要文件：** 新 Core 契约/结果模型、Infrastructure 提取实现、解析 fixture 测试。

**验收：**

- [ ] 先评估 .NET 原生/兼容库的许可证、维护状态和 AOT/WPF 兼容性，再记录依赖决策。
- [ ] 下载复用 SSRF、大小、超时和重定向限制；每主机限流。
- [ ] 输出只保留允许的标题、段落、列表、引用、图片和 HTTP/HTTPS 链接，移除脚本/样式/表单/iframe。

**验证：** 10+ 站点 fixture、编码、畸形 HTML、提示注入文本和净化测试。

**参考：** Folo `packages/readability` 的能力边界；禁止复制其 TypeScript/依赖源码，LenxTool 用 .NET 独立实现。

### P1-08：按 Feed 控制的全文抓取队列

**目标：** 管理员可为 Feed 配置正文策略，客户端以低并发后台补全内容。

**依赖：** P1-07、P0 管理目录。
**预计范围：** M。
**主要文件：** Feed 策略 DTO/迁移、全文队列服务、状态仓储、测试。

**验收：**

- [ ] 策略支持“不抓全文/打开时抓取/后台抓取”，默认不抓全文。
- [ ] 失败按主机退避，来源 403/robots/付费墙不绕过；Feed 停用立即停止新任务。
- [ ] RSS 自带完整正文时不重复抓取原网页。

**验证：** 策略切换、去重、取消、主机限流和失败恢复测试。

**参考：** Folo 全文阅读体验；LenxTool 的取消/任务可观察性。

### P1-09：阅读器增强与来源切换

**目标：** 在 RSS 正文、提取全文和原网页之间提供明确、安全的查看选择。

**依赖：** P1-06、P1-08。
**预计范围：** M。
**主要文件：** `RichArticleFormatter.cs`、`RichArticleView.cs`、`NewsCenterViewModel.cs`、解析/UI 测试。

**验收：**

- [ ] 清晰标识内容来源和提取时间，可切回 RSS 正文。
- [ ] 外链仍只交给受控 HTTP/HTTPS 打开，不在应用中执行脚本。
- [ ] 图片、标题、列表、引用和附件顺序稳定，朗读/供应商标记继续清理。

**验证：** 富文本回归、恶意 HTML、离线和真实 WPF 阅读手测。

**参考：** Folo 阅读布局；LenxTool 当前原生阅读器是实现基础。

### 检查点 P1-B

- [ ] 在线打开后断网，正文和已缓存图片仍可阅读。
- [ ] 全文抓取没有 SSRF、脚本执行、无限下载或付费墙绕过。
- [ ] 缓存预算和清理不会破坏收藏文章。

## 阶段 P1-C：AI 摘要与翻译

### P1-10：Feed AI 结果模型与缓存键

**目标：** 扩展现有 `ai_reports`，为条目摘要/翻译建立可失效的本地缓存。

**依赖：** P0 稳定条目 ID。
**预计范围：** S。
**主要文件：** AI 报告模型、`NewsRepository.cs` 或新仓储、SQLite 测试。

**验收：**

- [ ] 缓存键包含 entry ID、内容哈希、任务类型、目标语言、模型和提示版本。
- [ ] 原文变化后旧结果保留历史但不作为当前结果。
- [ ] 记录请求数/token/时间/错误，不记录 Key。

**验证：** 缓存命中、内容变更失效、历史查询测试。

**参考：** Folo summary/translation store；LenxTool 当前 `AiReport`/FTS5。

### P1-11：单条摘要与批量摘要

**目标：** 复用 DeepSeek 安全请求生成可取消的条目摘要。

**依赖：** P1-10。
**预计范围：** M。
**主要文件：** `IAiReportService.cs`、`DeepSeekReportService.cs`、`NewsCenterViewModel.cs`、测试。

**验收：**

- [ ] RSS/全文作为有长度上限的不可信 DATA，不得控制工具或系统提示。
- [ ] 单条和有界批量均可取消；相同缓存键不重复调用。
- [ ] 普通用户是否可手动生成由管理员策略和额度共同决定，但生成不修改共享目录。

**验证：** 假 HTTP、提示注入、超长内容、缓存、取消、429 和 token 测试。

**参考：** Folo `summary/store.ts`；LenxTool 单条解读/趋势报告实现。

### P1-12：条目翻译与双语阅读

**目标：** 对标题/摘要/正文生成本地缓存译文，并支持原文、译文、双语切换。

**依赖：** P1-10、Gate0 DeepSeek 字幕翻译边界。
**预计范围：** M。
**主要文件：** 新条目翻译服务、`NewsCenterViewModel.cs`、阅读器模板、测试。

**验收：**

- [ ] 保持段落顺序和链接目标，不让模型返回 HTML 直接执行。
- [ ] 长文分块可恢复，失败时原文始终可读。
- [ ] 目标语言、模型、内容哈希变化正确失效缓存。

**验证：** 分块/合并、乱序、缺块、取消、离线缓存和 WPF 手测。

**参考：** Folo `translation/store.ts`；LenxTool `SrtCodec`/字幕翻译的稳定顺序原则。

### P1-13：管理员 AI 策略

**目标：** 管理员按 Feed/分类配置是否允许手动摘要、自动摘要和自动翻译。

**依赖：** P1-11、P1-12、P0 管理目录。
**预计范围：** M。
**主要文件：** Worker 策略 migration/API、桌面策略 DTO、管理 UI、测试。

**验收：**

- [ ] user 只读策略；admin 写入有版本、RBAC 和审计。
- [ ] 默认关闭自动调用，展示预计用量和并发上限。
- [ ] 策略只在本地触发计算，AI 结果不上传 D1。

**验证：** 角色、版本冲突、禁用立即生效、额度不足和本地执行测试。

**参考：** Folo AI 自动处理体验；LenxTool Worker 额度/审计模型。

### 检查点 P1-C

- [ ] 摘要/翻译在内容未变化时不重复计费。
- [ ] 提示注入不能提升权限或触发外部操作。
- [ ] D1 检查确认没有新闻正文、译文或摘要。

## 阶段 P1-D：管理员自动化规则

### P1-14：受限规则模型与验证器

**目标：** 定义可审计、不可执行任意代码的条件/动作模型。

**依赖：** P0 目录、P1-01、P1-10。
**预计范围：** M。
**主要文件：** 新 Core 规则模型/验证器、Worker 策略 schema/API、Core/Worker 测试。

**首批字段：** Feed、分类、标题、作者、正文、语言、发布时间、是否含音频/视频。
**首批操作符：** equals、contains、regex（有长度/超时）、before/after、exists。
**首批动作：** 加标签、隐藏、标记已读、生成摘要、翻译、投递媒体、通知。

**验收：**

- [ ] 不允许脚本、任意 SQL、任意进程或任意 URL 请求。
- [ ] 规则有版本、优先级、启用状态和冲突顺序。
- [ ] regex 和文本输入有上限，非法规则在发布前拒绝。

**验证：** 规则组合、边界、灾难性 regex、防注入和角色测试。

**参考：** Folo `action/constant.ts`/`action/store.ts` 的字段-操作符-动作结构；LenxTool 重新定义更小的安全集合。

### P1-15：确定性规则解释器与运行记录

**目标：** 新条目写入后在本地幂等执行管理员规则。

**依赖：** P1-14。
**预计范围：** M。
**主要文件：** 新规则解释器、`automation_runs` 仓储、Core/SQLite 测试。

**验收：**

- [ ] 同一条目+规则版本只执行一次；重试不会重复投递外部任务。
- [ ] 冲突按优先级和明确动作顺序解决，运行结果可追踪。
- [ ] AI/媒体等异步动作只入受限队列，不在数据库事务内执行网络请求。

**验证：** 幂等、冲突、重启恢复、失败重试和大批量性能测试。

**参考：** Folo rule store；LenxTool 媒体持久化队列模式。

### P1-16：规则管理和模拟 UI

**目标：** 管理员可创建规则，并在发布前用最近条目模拟命中结果。

**依赖：** P1-14、P1-15。
**预计范围：** M。
**主要文件：** 新 `AutomationAdminViewModel.cs`、管理页 XAML、规则 API client、App 测试。

**验收：**

- [ ] 图形化选择字段/操作符/值/动作，不提供任意脚本编辑框。
- [ ] 模拟只读，不执行 AI、媒体或通知副作用。
- [ ] user 无入口且服务端拒绝写入；admin 发布记录审计。

**验证：** 模拟/发布差异、角色、可访问性和真实 WPF 手测。

**参考：** Folo 自动化规则交互；LenxTool 管理员 RBAC。

### 检查点 P1-D

- [ ] 规则运行幂等、可恢复、可审计，不允许任意代码/请求。
- [ ] 模拟模式无副作用。
- [ ] 普通用户不能创建或修改规则。

## 阶段 P1-E：媒体、搜索与保留

### P1-17：附件分类与安全播放

**目标：** 识别 RSS enclosure/Media RSS 中的音频、视频和图片，并提供受控播放/打开。

**依赖：** P0 解析器。
**预计范围：** M。
**主要文件：** `FeedEntry` 附件模型、媒体分类器、资讯 UI 模板、测试。

**验收：**

- [ ] 只信任允许的 HTTP/HTTPS URL 和验证后的 MIME/扩展组合。
- [ ] 未经确认不自动下载大媒体；显示大小未知和来源警告。
- [ ] 图片/音频/视频在不支持时回退为安全外链。

**验证：** MIME 欺骗、多 enclosure、无长度、私网 URL 和 UI 回退测试。

**参考：** Folo 音频/视频内容布局；LenxTool 媒体文件安全边界。

### P1-18：Feed 媒体投递到媒体工作台

**目标：** 管理员规则或用户查看操作可将允许的媒体附件创建为现有持久化任务。

**依赖：** P1-15、P1-17、Gate 0 完成。
**预计范围：** M。
**主要文件：** 新 Feed-media bridge、`MediaWorkbenchViewModel.cs`、任务模型/仓储、测试。

**验收：**

- [ ] 下载阶段有大小、类型、超时、取消和临时目录限制。
- [ ] 同一 entry/enclosure 不重复创建任务；来源条目可追溯。
- [ ] 转写、翻译、SRT 导出和历史完全复用 Gate 0 能力。

**验证：** 幂等投递、下载失败/取消、重启恢复和端到端小音频测试。

**参考：** Folo `useTranscription.ts` 的入口行为；LenxTool 现有 Groq/本地 Whisper 是唯一实现流水线。

### P1-19：统一搜索扩展

**目标：** FTS5 统一搜索 Feed、旧早报、热点、AI、字幕、标签和收藏。

**依赖：** P1-02、P1-10、P1-18。
**预计范围：** M。
**主要文件：** 搜索 DTO/仓储、`HistoryViewModel.cs`、历史页 XAML、测试。

**验收：**

- [ ] 支持实体类型、日期、Feed/分类、标签和收藏筛选。
- [ ] 结果排序确定，删除/更新实体同步更新 FTS，无幽灵结果。
- [ ] 字幕结果可跳转任务详情，Feed 结果可打开阅读器。

**验证：** 混合数据集、FTS 同步、筛选组合、分页和 WPF 手测。

**参考：** LenxTool 现有统一搜索；Folo 发现/搜索体验只作补充参考。

### P1-20：保留、清理与数据库维护

**目标：** 在新增正文、图片、AI 和字幕后落实可预测的存储上限。

**依赖：** P1-05、P1-19。
**预计范围：** M。
**主要文件：** `DatabaseMaintenanceService.cs`、资源清理服务、设置 UI、测试。

**验收：**

- [ ] 默认清理 180 天前无收藏/备注/标签/活动任务引用的内容。
- [ ] 图片缓存按预算/LRU；数据库清理后执行可取消维护，不阻塞启动。
- [ ] 设置页显示数据库/图片/模型占用，并允许安全清理预览。

**验证：** 引用保护、边界日期、取消、磁盘满和大库维护测试。

**参考：** LenxTool 既有 180 天规格和数据库维护；不是 Folo 直接移植。

### P1 最终检查点

- [ ] 私人阅读状态、全文、图片、摘要/翻译、规则、媒体和搜索端到端通过。
- [ ] 普通用户只消费管理员目录和策略，不能修改共享配置。
- [ ] 云端/D1 仍不保存正文、摘要、翻译、字幕或本地文件信息。
- [ ] 10k 条目、1k 收藏、混合媒体和离线场景性能可接受且 UI 无明显冻结。
- [ ] 文档、威胁模型、用户指南和测试报告同步后才标记 P1 完成。
