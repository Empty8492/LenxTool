# Lenx Tools 实施任务清单

任务按依赖和可运行垂直切片排序。每个任务完成后必须构建；含行为逻辑的任务先写失败测试再实现。

## 状态说明

状态最后核对日期为 **2026-07-21**。本文件用于保存任务拆分和完整验收条件：`[x]` 表示该项已满足全部验收条件，`[ ]` 表示未完成或仅部分完成。部分实现会直接写在任务后的括号中，不能仅凭复选框推断代码完全不存在。当前可交付能力、下一里程碑、运行配置和发布阻塞项以 [PROJECT_GUIDE.md 第 10 节](PROJECT_GUIDE.md#10-当前版本边界与交付状态)为准。

## 2026-07-21 路线扩展：管理员策展 RSS

已确认产品方向：只有管理员可以新增、编辑、删除、启停、分类、排序和导入 RSS/Atom；普通用户只能读取共享目录和内容。普通用户默认可保存不影响共享目录的本地已读、收藏、阅读进度和私人备注。授权必须由 Worker/D1 服务端执行，不能只隐藏客户端按钮。

以下文档是该扩展的可执行任务清单；所有复选框当前均为未完成：

| 执行顺序 | 专项计划 | 状态 |
|---:|---|---|
| 0 | [现有未完成项与 RSS 路线对齐](plans/EXISTING_BACKLOG_ALIGNMENT.md) | [ ] Gate0-01 字幕片段仓储已完成；Gate0-02～06 待完成 |
| 1 | [P0：管理员订阅与普通用户只读目录](plans/RSS_P0_ADMIN_CATALOG.md) | [ ] 未开始 |
| 2 | [P1：阅读增强、AI 与自动化](plans/RSS_P1_READING_INTELLIGENCE.md) | [ ] 未开始 |
| 3 | [P2：内容视图、导出与定时摘要](plans/RSS_P2_VIEWS_INTEGRATIONS.md) | [ ] 未开始 |

总体目标、架构、来源溯源、Folo 许可证边界和全局发布闸门见 [RSS 集成总路线图](plans/RSS_MASTER_ROADMAP.md)，管理员策展决策见 [ADR-001](decisions/ADR-001-admin-curated-rss.md)。Folo 仅作为行为参考，LenxTool 不依赖其未开源后端、`api.folo.is` 或客户端 SDK，也不复制其 AGPL 源码和受限图标。

### 新路线总检查点

- [ ] Gate 0：SRT 导入、批量翻译、片段持久化、译文/双语导出和历史闭环完成。
- [ ] P0：管理员可管理共享 Feed，普通用户只能同步、抓取和阅读；RBAC/审计/OPML/健康状态/旧库迁移通过。
- [ ] P1：私人阅读状态、全文/图片离线、AI 摘要/翻译、管理员规则、媒体投递和统一搜索通过。
- [ ] P2：多内容视图、统一导出适配器、本地定时摘要和通知通过。
- [ ] 每阶段更新规格、架构、威胁模型、用户指南和测试报告，并重新生成实际可验证制品。

## 阶段 0：只读审计与规格

- [x] 审计旧项目结构、入口、依赖、媒体分片、资讯源、安装脚本和凭据风险。
  - 验收：不修改 `L:\RealTimeTranslator`；记录可复用行为和明确禁用模式。
  - 验证：旧仓库工作树无本任务产生的变化。
- [x] 建立产品规格、架构、威胁模型和本任务清单。
  - 验收：目标、命令、结构、风格、测试、边界和完成定义齐全。
  - 验证：文档交叉引用一致。

## 阶段 1：工程基础

- [ ] 创建 solution、Core、Infrastructure、App 和两类测试项目。
  - 验收：Nullable 和 warnings-as-errors 开启；依赖方向正确。
  - 验证：Release build/test 通过。
- [ ] 建立 MVVM 基类、异步命令、导航契约、DI 组合根和应用生命周期。
  - 验收：窗口后台无业务逻辑；取消令牌随应用关闭传播。
  - 验证：命令状态与取消测试通过。
- [ ] 建立 `AppError`、HTTP 错误映射、日志脱敏和全局异常边界。
  - 验收：400/401/429/500/超时/断网为不同中文提示。
  - 验证：错误映射参数化测试通过。

### 检查点：工程基础

- [ ] Release 零警告构建；应用进入可导航首页；全部基础测试通过。

## 阶段 2：数据库与安全

- [x] 实现应用目录、SQLite 连接策略、迁移器和全部基础表/索引/FTS5。
  - 验收：首次启动建库；重复迁移幂等；迁移前自动备份。
  - 验证：临时数据库集成测试覆盖建库、FTS5、失败回滚。
- [ ] 实现资讯/任务/设置仓储的事务读写与 180 天保留策略。
  - 验收：收藏实体不会被清理；指纹唯一约束防重复。
  - 验证：事务和保留策略测试通过。
- [x] 实现 DPAPI SecretStore、日志脱敏、数据库备份/恢复及损坏检测。
  - 验收：API Key 不进入 SQLite/日志；恢复前备份当前库。
  - 验证：中文用户名/路径往返与损坏库测试通过。

### 检查点：本地数据垂直切片

- [x] UI 可保存非秘密设置、DPAPI 密钥并执行数据库备份；重启后可恢复。

## 阶段 3：精美应用外壳

- [ ] 实现语义主题、控件样式、矢量图标和深浅模式。
- [ ] 实现自适应 Shell、首页 Bento、导航和窗口最小尺寸。
- [ ] 实现 `Ctrl+K` 命令面板、焦点管理和减少动画。
  - 统一验收：深石墨侧边栏、暖白/完整深色内容区、无 Emoji 图标；900×620 到 4K、100%～200% DPI 可用；键盘完成命令搜索与执行。
  - 验证：资源/命令测试和四档 DPI 手测。

## 阶段 4：资讯中心

- [ ] 实现早报 RSS 客户端、标准化/指纹、缓存写入和首页展示。
- [ ] 实现多平台热点源适配器、单源隔离、筛选和去重。（已接入 TrendRadar 默认 11 个平台并保留 GitHub、Hacker News，共 13 个 NewsNow 来源；已完成域名校验、单源缓存替换、平台分组、多选筛选、选中来源报告和原文打开。原“用户自定义来源”不再按每用户自由配置实现，改由 P0 的管理员共享 Feed 目录取代。）
- [ ] 实现收藏、标签、备注和 FTS5 全局搜索。（FTS5 全局搜索界面已完成；收藏、标签和备注仍待完成。）
- [ ] 实现 WebView2 安全阅读、单条解读和每日趋势报告。（原生 WPF 安全阅读、单条解读和每日趋势报告已完成；未来其他 HTML 能力的 WebView2 安全承载仍待完成。）
  - 统一验收：在线刷新后断网仍显示缓存时间；一个源失败不影响其他源；报告可取消并持久化；外部 HTML/链接受控。
  - 验证：假 HTTP + SQLite + HTML 编码集成测试。

## 阶段 5：媒体工作台

- [x] 实现持久化任务队列、进度、取消、重试、恢复和历史。
- [ ] 实现音频提取适配器、文件探测和每任务临时目录。
- [ ] 实现 Groq、自备/共享代理、本地 Whisper 适配器和模型导入。
- [ ] 实现重叠分片、上下文续接、交接合并、重复消除和置信过滤。
- [ ] 实现 SRT 解析、批量翻译、SRT/双语 SRT/TXT 导出和打开目录。（Core 已完成 SRT 解析和原文/双语 SRT/TXT 编码，媒体任务已自动导出原文 SRT，字幕片段事务仓储已完成；SRT 导入、转写/导入结果持久化接线、批量翻译和译文/双语导出工作流仍待完成。）
  - 统一验收：中文/空格路径；模型不进安装包；长音频时间轴单调无交界重复；任务全程可取消重试。
  - 验证：状态机、分片、黄金 SRT、假 HTTP 和小媒体文件测试。

## 阶段 6：文档、数据工具与历史

- [ ] 实现 `IDocumentConverter` 与 Word COM 适配器。
- [ ] 实现 JSON 格式化/压缩/校验/排序/结构 Diff。
- [ ] 实现 Base64、URL、SHA-256、文本去重与空行清理。
- [ ] 完成历史、全局搜索、备份恢复和打开输出交互。（全局搜索、任务输出和备份恢复已完成；收藏与模型用量视图仍待完成。）
  - 统一验收：工具可离线使用；错误和模型用量可查看；危险恢复需确认。
  - 验证：单元/契约测试和端到端手测。

## 阶段 7：Cloudflare Worker + D1

> Worker 已有接口、D1 schema 和基础安全测试，但尚未部署，桌面客户端也未接线；以下任务继续保持未完成，直到远端配置、完整自动化验收和客户端闭环全部通过。

- [ ] 建立 Worker、D1 schema/migration、环境校验和健康检查。
- [ ] 实现邀请码注册、登录、短 access token、刷新令牌轮换和禁用。
- [ ] 实现管理员 RBAC、用户/邀请码额度配置和审计。
- [ ] 实现原子额度预留/结算/释放及管理员豁免。
- [ ] 实现 Groq/DeepSeek 允许列表代理、音频流式中转、超时和响应净化。
  - 统一验收：Secrets 不在仓库/D1；不落媒体与正文；并发额度测试通过。

## 阶段 8：更新系统

- [ ] 定义多镜像签名清单、离线签名工具和内置公钥验证器。
- [ ] 实现后台/手动检查、语义版本、最低版本与强制安全更新。
- [ ] 实现下载进度、大小/SHA-256/签名验证和安装交接。
  - 统一验收：篡改清单/安装包被拒绝；无更新与网络失败不阻塞启动。

## 阶段 9：安装与发布

- [ ] 完成固定 AppId 的 Inno Setup、WebView2 Runtime 检测/引导、快捷方式和数据保留。
- [ ] 完成自包含 win-x64、安装 EXE、便携 ZIP、SHA-256、清单和发布说明生成。
- [ ] 执行完整测试矩阵、覆盖升级/卸载保留测试和发布安全审查。
  - 统一验收：`Release/LenxTool_Setup.exe`、便携 ZIP、校验文件、使用说明齐全。

## 风险与缓解

| 风险 | 影响 | 缓解 |
|---|---|---|
| 公共热点接口变更或反爬 | 高 | 每源适配器、短超时、缓存、部分成功、可配置停用 |
| Word 转 PDF 保真与授权 | 高 | `IDocumentConverter` 隔离，清晰报告运行时能力，不承诺无损 |
| 本地 Whisper/native 体积 | 高 | 运行时与模型分离，模型导入，不进主安装包 |
| D1 并发额度竞争 | 高 | 条件更新/预留状态机、幂等请求 ID、并发测试 |
| 更新供应链攻击 | 高 | 离线签名、内置公钥、SHA-256、HTTPS、拒绝降级 |
| WPF/WebView2 DPI 与可访问性 | 中 | 语义样式、最小尺寸、自适应触发器、键盘/DPI 手测 |
| 外部工具与 CI 环境不足 | 中 | 能力探测、可操作错误、将依赖验证纳入发布脚本 |
