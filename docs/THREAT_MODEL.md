# Lenx Tools 威胁模型

## 资产

- 用户自备 Groq/DeepSeek Key、登录/刷新令牌、邀请码和管理员权限。
- 共享服务商 Key、每日额度、共享订阅目录、AI 策略、自动化规则及其版本、幂等写入记录、审计完整性和签名更新链路。
- 本地新闻/全文、图片缓存、AI 结果、私人阅读状态、收藏/标签/备注、字幕、规则运行与动作账本、媒体投递、应用内通知、用户文件路径和模型。
- 发布私钥（仅离线环境存在，永不进入客户端、Worker 或仓库）。

## 信任边界

1. WPF 输入、剪贴板、拖放文件与外部链接进入客户端。
2. 客户端与新闻源、Groq、DeepSeek、GitHub/镜像和 Worker 之间的 HTTPS。
3. WebView2 中不可信 HTML/Markdown 与本机应用之间的边界。
4. Worker 与匿名客户端、已认证 user、已认证 admin、D1、Groq/DeepSeek Secret 之间的边界。
5. 更新清单/安装包从发布环境进入客户端执行环境。
6. SQLite、DPAPI 文件、日志、模型和临时媒体位于同一 Windows 用户边界内。
7. 管理员提交的 Feed URL/显示元数据经 Worker 进入共享目录，再由各桌面端当作不可信配置读取；目录写路由本身不访问该 URL。
8. 管理员发布的 AI 策略和受限自动化规则经 Worker/D1 进入普通用户客户端；服务端负责 RBAC/版本，客户端仍必须对白名单字段、操作符、动作和大小再次验证。
9. 共享 AI/语音请求可在 Worker 请求生命周期内处理中转正文或媒体字节，但不得写入 D1、R2、KV、日志或审计；请求完成或取消后不保留内容。
10. 用户选择的 Obsidian Vault 位于应用数据目录之外；本机设置、管理员 ACTIVE 策略、持久导出队列与 Windows 文件系统共同构成写入授权边界，Vault 中其他文件始终视为不可修改的用户数据。
11. Eagle Web API 是客户端到本机 loopback HTTP 服务的独立信任边界：共享策略只授予类型许可，端点与当前资源库身份由本机设置和实时探测决定；Eagle 仍是第三方进程，其响应、资源库元数据和可变当前库均按不可信输入处理。

## STRIDE 与控制

| 威胁 | 主要滥用场景 | 控制与测试 |
|---|---|---|
| 冒充 | 窃取 refresh token、伪造管理员、伪造服务商 | access token 仅驻内存、refresh token 使用 DPAPI CurrentUser、刷新轮换与撤销、RBAC 每端点校验、TLS、请求 ID |
| 篡改 | 修改 D1 用量/目录、重放管理员写入、并发或旧快照覆盖新版目录、替换 SQLite 或更新包 | 参数化 SQL、事务、条件额度更新、目录 `If-Match`、幂等 key、服务端/本地单调版本、本地原子快照替换、签名清单、SHA-256、内置公钥 |
| 抵赖 | 管理员否认发码/改额/改 Feed，用户否认共享调用 | 仅记录元数据的审计事件、操作者/目标/目录版本/时间/请求 ID；禁止记录正文和 token |
| 信息泄露 | Key/摘要进入响应或日志，音频落云，目录泄露本机信息，WebView2 读取本机 | DTO 字段白名单、DPAPI CurrentUser、日志过滤、Worker 不落正文、禁脚本/受控导航、最小响应字段 |
| 拒绝服务 | 巨型 JSON/批次/目录、热点源挂起、无限 AI、注册爆破 | 逐端点输入上限、目录容量与响应上限、批次最多 100 项、流式处理、每源超时/取消、队列并发 1、速率限制、每日额度 |
| 权限提升 | 普通用户调用管理员目录端点、客户端篡改 role、用错误响应探测管理员资源 | 角色只以服务端签名令牌和 D1 当前用户为准；授权先于幂等/版本/资源检查；每个管理员写入服务端授权和审计 |

## 关键滥用用例

- 同一用户并发发起多个共享转写，试图全部在扣费前通过。服务端必须原子预留额度；无余额的请求在调用 Groq 前失败。
- 普通用户直接调用分类、Feed 或批量写端点，或篡改本地 `IsAdmin`。Worker 必须先验证 access token、D1 禁用状态和 admin 角色，再读取幂等记录、目录版本或目标资源；未登录返回 401，已登录非管理员返回 403。
- 同一桌面会话并发收到多个 401，试图触发 refresh token 重放或让请求无限重试。客户端以会话代次和单飞门闩只执行一次刷新，每个请求最多重放一次；旧 refresh token 被 Worker 原子轮换后永久失效，失败会话从内存和 DPAPI 清除。
- 攻击者在首次部署期间抢先初始化管理员或从终端历史窃取凭据。bootstrap 只接受至少 32 个字符的临时 Worker Secret，先验 secret 再检查账号状态，使用空库条件写入保证最多一个成功者，并受认证限流；受控脚本通过安全提示读取密码/token，不接受敏感命令行参数。成功验证登录后立即删除 Secret，端点随即表现为 404。
- 两个管理员基于同一旧目录并发修改，或攻击者重放成功请求。所有目录写入必须在同一事务内比较 `If-Match`、应用业务变更、递增全局版本、写审计和幂等结果；旧版本返回 409，同 key/同请求只重放原响应，同 key/不同请求返回 409。
- 管理页面在 401 后重放目录写入、409 后误自动覆盖，或远端写入成功但本地同步失败后继续使用旧版本。桌面客户端在一次逻辑操作中固定 `Idempotency-Key`，409 只刷新快照并要求重新核对，不自动重放；若新版本未同步到本地，则锁定全部后续写命令。角色在异步刷新中降级时，加载边界会再次检查会话并清空 ALL 投影。
- 客户端收到乱序、旧版本或中途损坏的目录快照。本地仓储必须在任何删除前拒绝版本倒退，并在同一 SQLite 事务中替换分类、Feed 和状态；约束失败或取消后读者继续看到上一完整版本，本地文章不随目录移除而删除。
- 恶意管理员或被盗管理员账号提交内网、环回、含凭据或重定向到保留地址的 Feed URL。目录写路由只做有界语法规范化、绝不抓取；发现服务默认只允许公网 HTTPS/443，DNS 结果经 IPv4/IPv6 分类后直接钉住到无代理连接，每次重定向重新校验，并限制连接/总超时、跳转、候选数、压缩/解压大小、MIME 和 XML DTD/实体。HTTP 与内网主机必须分别精确配置，内网 HTTP 同时满足两项。
- 恶意或失控的发现 provider 通过挂起、429、巨型/畸形 JSON、无界候选、伪造高置信来源证据或夹带危险元数据拖垮或欺骗聚合结果。协调器为每个 provider 独立限制等待/执行超时、并发、成功缓存、候选数和熔断窗口；Worker 目录响应在 512 KiB 内逐字段映射，候选 URL、文本、枚举、目录身份、证据和警告均先验证。全部证据及具名警告必须归属于实际 provider，异常文本不进入结果；一个来源失败只标记部分结果，调用方取消继续向下传播。RSSHub/外部平台在官方接口与条款审核前不注册。
- 服务端回滚、损坏或网络乱序让客户端收到比本地更旧的目录版本。服务端拒绝 `afterVersion` 超前请求；客户端拒绝版本倒退，只原子替换相同或更高版本的完整快照，失败或离线时保留最后一个完整版本。
- 恶意 RSS/AI 输出包含脚本、`file://`、自定义协议或提示注入。客户端只把内容当数据，编码/净化后渲染，不执行模型指令或自动打开链接。
- 恶意正文把伪造 system/tool 指令或 DATA 边界写进文章，试图劫持摘要/翻译。客户端只把有界正文放进不可信 DATA，模型无工具权限；结果按纯文本/类型化块校验并只写本机缓存，正文变化、目标语言、模型和提示版本共同隔离缓存键。
- 恶意 RSS/Atom 使用 DTD/外部实体、超大文档/条目集合、脚本块、危险 enclosure 或通过追踪参数制造重复身份。解析器在物化前限制字节和 XML 字符数、禁用 DTD/解析器、限制条目及字段集合；正文转为纯文本，链接仅接受 HTTP/HTTPS。稳定身份优先使用发布方 id/guid，其次使用仅删除明确追踪参数的 URL，避免破坏签名 query 或把不同内容错误合并。
- Feed 服务利用条件抓取制造永久空缓存、跨主机泄露验证器、重定向 SSRF、压缩炸弹或重试风暴。客户端仅在实际发送 ETag/Last-Modified 后接受 304，200 必须先事务写入条目再提交新验证器；跨 authority 跳转清除验证器并重新解析/钉住地址。压缩与解压大小、MIME、总超时、重定向、批量和并发均有上限；失败只保存固定错误码，并以 1 分钟至 6 小时指数退避，429 的 Retry-After 同样受上限约束。
- 恶意正文图片通过 DNS rebinding、私网重定向、SVG 脚本、伪造 MIME、巨型载荷或重复失败请求攻击客户端。图片缓存命中优先且不解析 DNS；网络未命中逐跳复用 Feed 地址策略与固定-IP 连接，禁用代理/Cookie/自动重定向，只接受 MIME 与魔数一致的有限位图格式。全局并发、单资源、每篇资源数和网络字节均有上限，切换文章传播取消；失败 URL 短期抑制重试并显示本地占位。
- 恶意全文页面通过 DNS rebinding、私网重定向、压缩炸弹、编码欺骗、巨型/深层 DOM、脚本/表单/iframe、危险协议或提示注入文本攻击客户端。全文下载逐跳复用 Feed SSRF 策略与固定-IP 连接，限制 HTML MIME、总超时、跳转、下载/解压大小和同主机并发；解析前后限制节点、正文块和文本总量。第三方 DOM 库只处理内存字符串且没有网络能力，输出是类型化白名单块，URL 只接受无凭据的 HTTP/HTTPS；提示注入只作为惰性正文文本保留，不获得工具或指令权限。
- 恶意管理员规则尝试注入脚本、SQL、命令、任意 URL、灾难性正则或超量条件/动作。Worker 与 Core 使用相同的封闭字段/操作符/动作语义和显式数量/长度上限；无参数动作拒绝载荷，正则拒绝反向引用/前后查找并在本机使用 100 ms 非回溯引擎。发布采用 admin RBAC、独立规则集 `If-Match`、幂等键、不可变版本和最小审计；普通用户只能读取 ACTIVE。
- 攻击者重放规则、租约或进程内通知，试图重复隐藏、标记、调用 AI、下载媒体或创建通知。确定性计划使用条目/规则版本和稳定动作幂等键；运行账本、动作租约、媒体来源台账和通知表分别拒绝重复，旧租约持有者不能提交，重启后只恢复未完成状态。
- 恶意 Feed 标题、抓取错误或 AI 结果试图借系统健康/任务完成通知泄露正文、摘要、异常详情、URL、令牌，或以重复失败淹没收件箱。三类通知模型只接受封闭类别、有界本地关联、规范化标题和来源，不存在正文/结果/详情/URI 字段；规则动作、失败周期与摘要任务分别提供稳定幂等键，只有首次落库才触发进程内事件。通知发布失败与原任务结果隔离，类别筛选不写数据库，已读只更新本地 SQLite。
- 恶意管理员或被篡改的智能视图响应试图注入 SQL、正则、脚本、URL、正文条件，越权读取停用视图，上传私人已读/收藏状态，或用旧版本覆盖离线缓存。Worker 仅接受封闭筛选 DTO 和规范 GUID/枚举/长度/窗口，服务端 RBAC 隔离 ACTIVE/ALL 与全部写入，独立条件版本、幂等键和事务守卫防止竞态。D1 表/版本 JSON 不含内容或私人状态字段；桌面再次验证 scope、单调版本、启用状态、唯一 ID、数量和响应大小，失败保留最后有效缓存。已读/收藏只在本机把定义投影为查询，不回传命中结果。
- 恶意集成策略或个人目标试图使用通配符、IDN/尾点混淆、凭据 URL、非标准端口、字面 IP、私网 DNS、重定向或快速重复测试形成 SSRF 与凭据泄露。Worker 只接受封闭类型；Obsidian/Eagle 必须使用空主机列表，其余七种启用类型必须提供精确 DNS 主机。ACTIVE/ALL 与 PUT 由服务端角色隔离并以独立版本、幂等和事务守卫提交；D1 不含个人 TargetId、URL、凭据或健康结果。普通网络目标再次要求 HTTPS/443、无 userinfo/query/fragment、精确主机命中并检查全部 DNS 地址；提供商专用探针只能接收已批准端点和固定地址，健康 DTO 不承载响应、异常、请求头或完整 URL，并受 8 秒超时、调用方取消、30 秒目标冷却和全局并发 2 限制。Eagle 不复用该公网例外，而走下述专用 loopback 边界。个人凭据使用哈希槽位进入 DPAPI CurrentUser，设置页不回读且保存后清空输入；未安装的网络适配器不会产生第三方出站请求。
- 恶意 Obsidian 配置或 Feed 文本试图把导出写到 Vault 外、覆盖用户笔记、利用 UNC/device/network、磁盘根、`..`、ADS、保留名、junction/symlink，或借 Markdown 图片/链接、自定义 URI、代码围栏和模板递归替换形成点击执行、资源加载或内存/磁盘放大。目标只保存为单条版本化 JSON；队列使用不含 Vault 明文的配置修订标识，并在执行时重读当前配置。版本化任务在目录操作前必须精确匹配当前规范化配置，过期作用域以非重试 `Conflict` 关闭；只有预版本 `default` 任务保留迁移兼容。Vault 必须是已存在且非磁盘根的绝对本地目录，相对子目录和文件名分别执行 Windows 路径规范化；目录创建、同目录临时文件创建和原子移动前都会重复检查包含关系与 reparse point。模板最多 64 KiB、五种占位符每种最多一次并单遍非递归替换；实际输入最多 8 MiB、最终 UTF-8 最多 12 MiB。HTML 在建 DOM 前受标记预算保护，解析器本身限制嵌套，解析后再限制 16,384 个来源节点、128 层深度与深度加权工作量；特殊代码/脚本节点不会先按普通文本扩容。Feed 文本按纯文本转义 Markdown 控制字符，代码围栏按内容动态加长，只有渲染器校验过的 HTTP(S) 来源链接可成为 angle-delimited 目标；标签作为逐项转义的复数 YAML `tags` 写入。渲染和输出预算通过前不创建目标目录或复制缓存资源；每个内容版本只创建确定性新文件，从不覆盖或读取 Vault 其他文件。适配器只有在明确本机设置、ACTIVE 策略和阅读器按钮入队同时成立时执行，并在实际写入前重读配置/策略；不读取模板文件、不安装插件，也不存在 `obsidian://` 执行面。暂时的设置存储或目录 I/O 故障可重试，权限、无效配置、过期作用域和撤销策略失败关闭。
- 恶意 Eagle 端点、伪协议本机服务、远程图片或资源库切换试图绕过授权、诱导代理/重定向出站、制造超限响应/图片、把图片 URL 交给第三方下载、重放新增或泄露资源库路径。端点必须是带显式端口的数字 loopback HTTP 根地址，拒绝 userinfo、路径、查询和片段；专用 handler 禁用系统代理、自动重定向、Cookie 与自动解压，并限制连接、请求 8 秒和 JSON 256 KiB。只有 ACTIVE Eagle 策略、本机设置与显式阅读器动作同时成立才探测；`app/info`/`library/info` 必须符合 Windows Web API V2 能力。资源库路径立即转成不透明修订，原文不保存、不显示、不挂异常链；端点和资源库共同进入队列作用域。导出持有当前端点的进程内代际租约，同进程保存新端点会等待；资源库在查询、下载与新增边界重复探测，已观察到的变化以 `Conflict` 关闭。图片先经允许 URL、验证媒体类型、12 MiB 声明/实际流、MIME 与魔数交叉检查，只把 data URI 和有界元数据交给 Eagle。稳定自定义 ID、写前查询与不确定 POST 后复查阻止崩溃/并发重放重复创建；复查未知、408、429 和 5xx 保持可重试。响应正文、请求头、图片正文和本机资源库信息不进入界面或任务历史。残余风险是官方 `item/add` 不接受资源库身份/条件参数：探测与写入无法原子绑定，写后持续切库虽会报 `Conflict`，但可能发生的误写不可撤销，ABA 切库不可观测；另一个 LenxTool 进程直接修改设置也不受本进程代际门协调。真实验收期间必须保持 Eagle 当前资源库不变。
- `SendToMedia` 被用于 SSRF、MIME/扩展名欺骗、巨型文件、慢速流或临时文件残留。执行前重新确认条目及 Feed/分类启用状态，只接受 P1-17 白名单音视频；下载逐跳钉住公网地址，限制重定向、并发、512 MiB、10 分钟和专用临时目录，并以容器魔数复核。拒绝、失败、超限和取消不创建媒体任务且清理临时文件；成功才原子登记来源和任务。
- 恶意音频条目借页面选择自动连接追踪源、伪装格式进入播放器、用迟到事件污染另一条目的进度，或诱导用户无确认下载。音频页首次进入和选择只读取本地条目；仅明确播放才接受附件分类器判定为允许 URL、完整类型验证且为 Audio 的 enclosure。切换/释放关闭旧播放器，ViewModel 还按来源 URL 丢弃迟到状态；进度只写本机档案。强制 Audio 无可信 enclosure、系统解码失败或断流时不直接打开附件，只能经两步确认把安全原文交给默认浏览器。转写必须另行点击并继续通过 `SendToMedia` 的逐跳网络、容器、大小、取消和幂等边界。
- 恶意视频条目借列表选择加载追踪播放器、用伪封面绕过图片边界、隐藏巨型/未知下载、耗尽磁盘，或把容器签名正确但本机无法处理的文件登记为任务。视频页不嵌入媒体/网页播放器且选择不触网；封面只接受完整验证的 Image enclosure 并继续走固定地址图片链。原文外开为两步确认且不直接打开 enclosure。投递计划显示大小/上限、固定目标和空间，未知或至少 20 MiB 再确认；确认前、联网前和响应长度可知时分别重检，保留 64 MiB，取消清理临时文件。下载后必须由 Media Foundation 读取到有效音轨才移动并登记，失败时零任务。
- 保留任务误删用户收藏、标签、私人阅读状态、仍在处理或等待导出的条目，或条目/FTS 在中途失败后不一致。清理只能从设置页先预览再显式确认，统一候选 SQL 排除 `favorites`、`entity_tags`、任意 `user_entry_states`、全文/AI/规则/媒体活动任务，以及状态为 `QUEUED`/`RUNNING` 的导出任务引用；导出进入终态后才恢复正常保留判断。每批最多 5000 条，关联账本、资源索引、条目和 FTS 在同一 SQLite 事务边界内处理。图片文件删除与缓存写入/LRU 维护共用互斥门闩，只移除索引不再引用的内容哈希。取消停止后续批次；磁盘空间不足或数据库忙碌只跳过 `VACUUM`，不会把已完成清理伪装成完整回滚。
- 恶意 OPML 使用 XXE、深层 outline、超大属性/文件、重复 URL 或重定向后的目录冲突，或诱导导出凭据。客户端在物化前限制为 2 MiB、禁用 DTD/外部实体并限制字符数、outline、Feed、深度和属性；预览默认不提交，重复/冲突/无效项不可选。选中项在批量写入前逐项复用 SSRF 防护发现服务并对最终 URL 再分类，任一失败整批零提交；Worker 限制 1～100 个操作并原子比较版本、写资源/审计/幂等结果。导出只允许公开目录字段，不读取账号令牌、密钥、正文或本地抓取状态。
- 恶意或巨量缓存条目试图冻结时间线、借阅读界面触发目录写入，或用快速筛选制造乱序结果。时间线每页最多读取 50 条、关键词最多 200 字符，列表采用回收虚拟化；筛选代次丢弃过期异步页，追加结果按 ID 去重。页面只依赖 ACTIVE 目录和条目只读查询，不暴露订阅编辑命令；条目正文继续按不可信纯文本交给原生只读阅读器，外链仅允许 HTTP/HTTPS 且不会自动打开。
- 大型离线库、全文索引或保留清理被用于冻结 UI。查询和统一搜索采用有界页大小/关键词/筛选，容量扫描在窗口显示后后台执行；清理先预览再确认，按最多 5000 条事务批次提交并传播取消。P1 最终验收以 10,000 条 Feed、1,000 个收藏和混合媒体验证离线查询/搜索 2 秒、预览 10 秒、清理 60 秒预算。
- 首页聚合被停用 Feed、旧早报重复迁移或损坏 favorites 数据污染。首页查询显式使用 ACTIVE Feed 投影，旧 `news_articles` 仅作为本地兼容读取，展示层按规范 URL/内容指纹去重；favorites 只读取计数，不把私人收藏状态上传云端。空目录兼容种子只预填管理员表单，仍必须经过安全发现、版本和授权写入。
- 管理员健康页被恶意 Feed 状态或重复点击利用来泄露响应/网络细节或制造重试风暴。状态仓储只返回本地固定错误码，ViewModel 映射为有限类别并隐藏响应内容；健康列表读取所有目录 Feed 但不包含令牌或正文，重试命令由单一异步命令串行化，并继续经过 Feed 刷新的 SSRF、大小、超时、解析和单 Feed 门闩策略。
- 攻击者替换 GitHub 上的 JSON 或安装包。任一签名、大小、哈希不匹配即中止，且不能由远端配置替换客户端信任公钥。
- 用户导入伪装成模型的任意大文件。先检查文件名、大小、可读性和模型头，不把路径拼接进 shell；超限拒绝。
- 数据库迁移中断或磁盘已满。迁移前一致性备份、事务回滚、保留原库并给出恢复入口。

## 隐私最小化

云端允许字段仅限账号 ID、用户名规范化值、密码摘要、角色、邀请码摘要/策略、额度、聚合用量、令牌摘要、禁用状态、共享分类/Feed 配置、目录与规则集版本、AI 策略、受限规则定义/不可变版本、幂等请求摘要/结果和安全审计元数据。共享 Feed 配置仅含 URL、显示名、分类、视图/全文/刷新/排序/启用策略；规则仅含白名单字段、操作符、值和动作，不含匹配到的条目数据。禁止字段包括新闻正文/全文、AI 摘要或译文、字幕、音频/视频、用户文件名、设备名、完整本地路径和私人阅读/收藏状态。

## 安全发布门禁

- 搜索仓库中疑似 Key、密码、私钥和 bearer token；命中必须人工解释或移除。
- 依赖漏洞与许可证审查无未处置的高危项。
- Worker 对认证、授权、401/403 区分、限流、并发额度、目录/规则版本冲突、幂等重放/错用、目录与规则 ACTIVE/ALL 隔离、超前版本拒绝和响应字段白名单有自动测试。
- 客户端对错误映射、DPAPI、账号恢复/刷新风暴/退出/脱敏、角色降级与异步降权竞态、Feed schema v2 原位升级与失败回滚、目录原子替换/版本倒退/删除保留、ACTIVE/ALL 范围升级、错误 304、10 MiB 上限、离线/超时/取消保留与退避、管理员写入版本/幂等头、401 同键重放、409 不覆盖、写后同步失败锁定、SSRF 地址分类/DNS rebinding/重定向/压缩炸弹/XXE、统一发现 provider 超时/429/畸形响应/证据归属/缓存/熔断/取消/部分失败、图片缓存命中/私网跳转/SVG 与伪 MIME/预算/取消/失败抑制、音频显式播放/本地进度/旧事件隔离/断流回退/转写取消与幂等、全文下载私网阻断/大小/超时/同主机并发/编码/DOM 上限/白名单净化、RSS/Atom 有界解析与稳定身份、条件抓取顺序/验证器隔离/并发去重/指数退避、Feed 健康状态查询/错误脱敏/手动重试门闩、Feed 时间线分页/筛选/10k 缓存首屏/回收虚拟化/只读布局、数据库迁移、HTML 编码和更新验签有自动测试。P0-C 另以 20 个独立 RSS/Atom fixture 验证中文、ISO-8859-1、UTF-16 LE/BE 与常见格式变体；P1-07 以 12 个文章 fixture 验证常见 DOM、中文、畸形 HTML、提示注入文本和净化，并复用网络安全拒绝测试作为发布门禁。目录同步和已知目录发现只取得受限同源 GET 能力；独立管理写服务不暴露 token，最终授权仍由 Worker 执行。
- 发布私钥仅由离线签名步骤读取；CI/客户端只读取公钥。

## P0 终验安全证据（2026-07-24）

`p0-final-acceptance.test.ts` 在真实 workerd/D1 环境中验证 bootstrap/login 后的管理员目录生命周期、普通用户只读和六类写端点 403；审计只保存操作者、目标、动作、目录版本和请求 ID，不包含 URL 正文、令牌或响应内容。完整回归同时通过 OPML 的 XXE/深度/大小限制、断网与坏源隔离、旧 schema 迁移回滚和 10k 条目分页门限，作为 P0 发布前的安全证据。

## P1 终验安全证据（2026-07-27）

`p1-final-acceptance.test.ts` 在真实 workerd/D1 中验证管理员可发布目录、AI 策略和受限规则，普通用户对三类写入均得到 403 且目录/规则版本保持不变；测试逐表核对 D1 应用表和字段，并以正文、摘要、译文、字幕和本地路径哨兵确认没有内容落库。`P1FinalAcceptanceTests` 在重开的真实 schema v17 SQLite 上验证 10,000 条 Feed、1,000 个收藏、全文/AI/规则/媒体活动引用和混合媒体在清理后仍保持边界完整。完整 Release 回归为 .NET 648/648、Worker 52/52、strict typecheck 与 0 警告构建；开发依赖的 4 个现有 high 审计项仍需在正式发布前通过 Cloudflare 工具链升级或风险处置关闭，因此 P1 完成不自动关闭正式安全发布门禁。

## DISC-03 发现安全检查点（2026-07-28）

47 项发现链路定向测试验证 direct provider 继续阻断私网、环回、混合 DNS、重定向重绑定与未授权 HTTP，并固定已验证地址；统一协调器覆盖独立超时/并发、成功缓存、熔断恢复、429、畸形/空目录项、重复候选、来源证据伪造、provider 集合快照、调用方取消、部分成功和全源不可用。App DI 测试确认默认只注册 direct 与 Worker known-catalog provider，RSSHub/外部平台未启用。完整门禁为 .NET 722/722、Worker 63/63、strict typecheck、npm audit 0 漏洞及 Release 构建 0 警告/0 错误。

## DISC-06 发现最终安全证据（2026-07-28）

最终检查点在真实 workerd/D1、真实 schema v18 SQLite 和 Release WPF 回归上关闭统一发现边界。Worker 的 5,000 条容量测试与响应 allowlist 证明关键词索引只返回 Feed 标识、规范地址、标题、站点、分类、视图、启用状态、更新时间和类型化来源证据；既有 D1 schema/哨兵扫描继续确认没有正文、摘要、译文、字幕、本地路径或私人阅读状态。发现 Worker 和桌面协调器没有日志调用；原始 `request.url` 只用于 2 KiB 长度限制，解析后的查询仅进入参数化 SQL 和规范化响应，认证头只影响 `Vary`，异常和 provider 载荷被映射为类型化状态，因此完整令牌、查询内容和本地路径不会进入发现日志。管理员发布仍复用原目录写端点，真实 workerd/D1 覆盖普通用户 403、同版本并发单赢家、版本冲突和最小审计，桌面覆盖冲突只刷新不重放、网络结果未知时锁定。直接探测继续覆盖私网/环回/混合 DNS/重绑定、逐跳重定向、XXE、压缩炸弹、超限和超时；慢来源只产生部分结果状态，不吞掉健康来源。最终门禁为 .NET 755/755、Worker 64/64、strict typecheck、NuGet/npm 0 漏洞和 Release 构建 0 警告/0 错误。

## P2-03 音频视图安全证据（2026-07-28）

音频页的选择、筛选和惰性初始化测试确认不会打开媒体源或创建任务；只有显式播放才把经过附件 URL、MIME/扩展一致性和 Audio 分类验证的来源交给系统媒体适配器，适配器入口还会独立重验同一组合。断流、取消、暂停、旧源迟到事件、重启进度和接近结束重播语义由隔离假播放器验证，本地进度不进入 Worker 或新增云端字段。强制 Audio 但无可信 enclosure、系统播放失败或保留地址原文会分别进入二次确认回退或彻底禁用。显式转写继续复用 P1 Feed-media 的逐跳固定地址、重定向、类型/签名、512 MiB、超时、临时文件清理与 entry/source 幂等测试；取消不会发布任务或导航。真实 WPF 以 1,000 条音频覆盖 900×620、200% 缩放、虚拟化和可访问按钮。最终门禁为 .NET 765/765、Worker 64/64、strict typecheck、NuGet/npm 0 漏洞和 Release 构建 0 警告/0 错误。

## P2-04 视频视图安全证据（2026-07-28）

视频页惰性查询、选择零网络、可信封面和无内嵌播放器由 ViewModel、XAML 与真实 WPF 共同验证；伪 MIME 海报、保留地址原文和直接 enclosure 外开均不能成为操作。计划服务覆盖小文件、20 MiB 阈值、未知大小、512 MiB 上限、目标目录、可用空间、已有同源任务和确认后二次复核。Infrastructure 在连接前为声明值或最大值加 64 MiB 预留空间，并在响应长度可用时再次检查；取消与异常继续清理临时文件。视频在任务登记前以保留真实扩展的临时文件交给 `MediaFoundationReader`，只有存在本机可读音轨时才移动到持久目录；探针失败测试确认零任务、零残留。真实 WPF 以 1,000 条视频覆盖 900×620、200% 缩放、虚拟化和可访问按钮。最终门禁为 .NET 784/784、Worker 64/64、strict typecheck、NuGet/npm 0 漏洞和 Release 构建 0 警告/0 错误。

## P2-05 通知流安全证据（2026-07-28，阶段）

schema v19 迁移、仓储和发布器测试确认历史通知无损归类、三类值域封闭、稳定 ID 并发幂等、跨重启与本地已读往返。抓取异常连续两次只形成同一失败周期键，通知标题不携带 HTTP 状态或异常详情；摘要完成只引用任务/条目/Feed 和标题，不包含生成内容。发布器规范化控制字符和长度，重复注册不再次推送，通知发布失败不会反向改变已保存的抓取状态或 AI 完成状态。ViewModel 与 XAML 结构测试确认类别筛选不修改未读状态，模板不绑定正文、摘要或 URL。当前门禁为 .NET 789/789 和 Release 构建 0 警告/0 错误；共享智能视图边界将在 P2-05 后续片完成。

## P2-05 智能视图安全证据（2026-07-29，完成）

真实 workerd/D1 测试覆盖 0009 表约束、管理员创建/停用/删除、普通用户 ACTIVE 只读与写入/ALL 403、条件版本冲突、幂等重放、旧客户端版本超前、非法 GUID/枚举/窗口/未知字段/脚本/URL 拒绝、不可变版本与最小审计。全库表/列扫描继续拒绝正文、摘要、译文、字幕、本地路径或令牌字段。Core 验证覆盖规范化、封闭查询映射、私人档案只在本机注入、无效引用/窗口/关键词和停用定义阻断；schema v20 真实 SQLite 覆盖事务替换、跨重启、单调版本、停用快照拒绝与同步时间条件更新。桌面传输覆盖 ACTIVE 增量路由、非递增/错误 scope 保留缓存、管理员请求只有 filter 且使用独立版本头，以及 DELETE 空正文。普通用户 UI 套用前重读最新本地快照，被删除定义不会从旧内存执行，缓存重读失败时拒绝套用，同步失败只使用最后有效缓存；临时筛选不上传。管理员页只提供封闭控件并在 Worker RBAC 之外再次隐藏入口；版本冲突不自动重放，远端请求或本地缓存同步期间角色撤销后都不会重新填充管理状态。当前门禁为 .NET 821/821、Worker 70/70、strict typecheck 和 Release 构建 0 警告/0 错误。

## P2-11 Obsidian 安全证据（2026-07-30）

目标存储、路径策略和适配器测试覆盖缺失/损坏配置、空模板规范化、UNC/device/network/磁盘根、目录逃逸、ADS、Windows 保留名、尾随空格/点、junction/symlink、Vault 消失、权限拒绝和暂时 I/O；错误只落入封闭的 `InvalidRequest`、`AccessDenied`、`DestinationUnavailable` 或 `Conflict`，仅暂时 I/O 可重试。版本化队列作用域不泄露路径，Windows 等价路径保持同一标识，实际目标或渲染配置变化生成新标识；旧作用域在任何写盘前冲突关闭。Markdown 测试覆盖 front matter/标签转义、原始链接/图片/自定义 URI、危险右括号目标、内联与围栏代码边界、标点扩容、8 MiB 输入和 12 MiB 输出；标记洪泛、解析期深度、16,384 节点、128 层深度与深度加权工作量均受限，输出失败时目标目录和缓存资源保持未创建。App/DI/真实 WPF 覆盖仅在本机设置、ACTIVE 策略与指定条目显式动作三者成立时入队，启动、选择、刷新和保存设置均零写盘；队列并发复活与保留清理继续保护活动引用。完整门禁为 .NET 1005/1005、Worker 75/75、strict typecheck 和 Release 构建 0 警告/0 错误。

## P2-12 Eagle 安全证据（2026-08-03）

Worker/Core 测试固定 Obsidian/Eagle 空主机许可及两种本机集成的任意非空主机拒绝，D1 仍只保存九种策略、开关和主机 JSON。目标存储、API 客户端和导出器覆盖非法端点、未安装/错误端口、版本/平台/资源库元数据不兼容、超限或畸形 JSON、响应正文脱敏、禁代理/跳转/Cookie、408/429/5xx、调用方取消、POST 不确定结果复查和复查未知可重试。端点代际测试确认同进程保存必须等待活动导出释放；资源库修订测试证明原始路径不离开探测边界，同端点切库产生不同队列/幂等作用域，查询或下载期间已观察到的切换会关闭，新增期间保持到写后探测的切换会报告 `Conflict`。这些测试不证明原子选库：官方 `item/add` 缺少库身份，误写无法回滚且 ABA 切换不可观测。图片测试覆盖受阻/未验证/不支持附件、声明及实际 12 MiB、MIME 欺骗、五种魔数、data URI、标题/来源/标签映射、稳定 ID、已有条目跳过过期下载和重复投递收敛。App/DI/XAML 覆盖设置保存/测试共享忙门、策略撤销、超时阶段、释放取消、行内与详情命令参数、显式入队、可访问实时状态和启动/选择/刷新零导出。NuGet/npm 在线审计均为 0，Release 构建 0 警告/0 错误；无受控真实 Eagle 连通证据，P2-D 对应人工检查点仍未关闭。
